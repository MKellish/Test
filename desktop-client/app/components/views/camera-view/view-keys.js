/* 
 * subviewKeys
 * Holds the subviewKeys that map to a given 
 * CameraView subview component
 */
const subviewKeys = {
	ACQUIRE: "acquire",
	VALIDATE: "validate",
	IMAGE_NOTES: "notes",
}

export {
	subviewKeys,
}
